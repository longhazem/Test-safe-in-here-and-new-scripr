local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local SoundService = game:GetService("SoundService")
local Players = game:GetService("Players")

local function CleanupOldUI()
    local oldUI = (gethui and gethui():FindFirstChild("TOKAIHUB")) or game.CoreGui:FindFirstChild("TOKAIHUB")
    if oldUI then oldUI:Destroy() end
end
CleanupOldUI()

local Library = {}

function Library:CreateWindow()
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "TOKAIHUB"
    screenGui.Parent = (gethui and gethui()) or game.CoreGui
    screenGui.ResetOnSpawn = false
    screenGui.IgnoreGuiInset = true -- Để UI không bị lệch bởi thanh hệ thống Roblox

    -- Cấu hình màu sắc
    local MainColor = Color3.fromRGB(255, 192, 203)
    local TextColor = Color3.fromRGB(100, 50, 60)

    -- KHUNG CHÍNH (Sử dụng Scale để tương thích mọi màn hình)
    local main = Instance.new("Frame")
    main.Name = "MainFrame"
    main.Size = UDim2.new(0.5, 0, 0.6, 0) -- Chiếm 50% rộng, 60% cao màn hình
    main.Position = UDim2.new(0.5, 0, 0.5, 0)
    main.AnchorPoint = Vector2.new(0.5, 0.5)
    main.BackgroundColor3 = MainColor
    main.BackgroundTransparency = 0.2
    main.Parent = screenGui
    
    -- Giới hạn kích thước tối thiểu và tối đa (tránh quá to trên PC hoặc quá nhỏ trên Mobile)
    local uiSizeConstraint = Instance.new("UISizeConstraint", main)
    uiSizeConstraint.MaxSize = Vector2.new(650, 420)
    uiSizeConstraint.MinSize = Vector2.new(360, 240)

    local mainCorner = Instance.new("UICorner", main)
    mainCorner.CornerRadius = UDim.new(0.05, 0) -- Bo góc theo tỉ lệ

    local mainStroke = Instance.new("UIStroke", main)
    mainStroke.Color = Color3.fromRGB(255, 255, 255)
    mainStroke.Thickness = 2
    mainStroke.Transparency = 0.5

    -- PHẦN PROFILE (LEFT)
    local profileFrame = Instance.new("Frame", main)
    profileFrame.Size = UDim2.new(0.45, 0, 0.45, 0)
    profileFrame.Position = UDim2.new(0.04, 0, 0.06, 0)
    profileFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    profileFrame.BackgroundTransparency = 0.6
    Instance.new("UICorner", profileFrame).CornerRadius = UDim.new(0.1, 0)

    local pfp = Instance.new("ImageLabel", profileFrame)
    pfp.Size = UDim2.new(0.35, 0, 0.7, 0)
    pfp.Position = UDim2.new(0.05, 0, 0.15, 0)
    pfp.SizeConstraint = Enum.SizeConstraint.RelativeYY -- Giữ ảnh luôn vuông
    pfp.Image = Players:GetUserThumbnailAsync(Players.LocalPlayer.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size150x150)
    pfp.BackgroundColor3 = MainColor
    Instance.new("UICorner", pfp).CornerRadius = UDim.new(1, 0)

    local welcomeMsg = Instance.new("TextLabel", profileFrame)
    welcomeMsg.Size = UDim2.new(0.55, 0, 0.9, 0)
    welcomeMsg.Position = UDim2.new(0.4, 0, 0.05, 0)
    welcomeMsg.BackgroundTransparency = 1
    welcomeMsg.Text = "Welcome,\n" .. Players.LocalPlayer.DisplayName .. "!"
    welcomeMsg.TextColor3 = TextColor
    welcomeMsg.Font = Enum.Font.GothamBold
    welcomeMsg.TextScaled = true -- Tự động chỉnh cỡ chữ theo khung
    welcomeMsg.TextXAlignment = Enum.TextXAlignment.Left

    -- PHẦN ABOUT (RIGHT)
    local aboutFrame = Instance.new("Frame", main)
    aboutFrame.Size = UDim2.new(0.4, 0, 0.45, 0)
    aboutFrame.Position = UDim2.new(0.55, 0, 0.06, 0)
    aboutFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    aboutFrame.BackgroundTransparency = 0.6
    Instance.new("UICorner", aboutFrame).CornerRadius = UDim.new(0.1, 0)

    local aboutText = Instance.new("TextLabel", aboutFrame)
    aboutText.Size = UDim2.new(0.9, 0, 0.8, 0)
    aboutText.Position = UDim2.new(0.05, 0, 0.1, 0)
    aboutText.BackgroundTransparency = 1
    aboutText.Text = "TOKAI HUB\nMobile Support: 100%\nResolution: 360p+"
    aboutText.TextColor3 = TextColor
    aboutText.Font = Enum.Font.Gotham
    aboutText.TextScaled = true
    aboutText.TextWrapped = true

    -- SIDEBAR (Điều hướng)
    local sidebar = Instance.new("Frame", main)
    sidebar.Size = UDim2.new(0.1, 0, 0.8, 0)
    sidebar.Position = UDim2.new(1.02, 0, 0.1, 0)
    sidebar.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    sidebar.BackgroundTransparency = 0.5
    Instance.new("UICorner", sidebar).CornerRadius = UDim.new(0.3, 0)

    local sideLayout = Instance.new("UIListLayout", sidebar)
    sideLayout.Padding = UDim.new(0.05, 0)
    sideLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    sideLayout.VerticalAlignment = Enum.VerticalAlignment.Center

    -- TAB CONTAINER (Nơi chứa các nút chức năng)
    local tabContainer = Instance.new("Frame", main)
    tabContainer.Size = UDim2.new(0.92, 0, 0.4, 0)
    tabContainer.Position = UDim2.new(0.04, 0, 0.55, 0)
    tabContainer.BackgroundTransparency = 1

    local pages = Instance.new("Frame", tabContainer)
    pages.Size = UDim2.new(1, 0, 1, 0)
    pages.BackgroundTransparency = 1

    function Library:CreateTab(name, iconText)
        local page = Instance.new("ScrollingFrame", pages)
        page.Size = UDim2.new(1, 0, 1, 0)
        page.BackgroundTransparency = 1
        page.Visible = false
        page.ScrollBarThickness = 2
        page.ScrollBarImageColor3 = TextColor

        local layout = Instance.new("UIGridLayout", page)
        layout.CellSize = UDim2.new(0.48, 0, 0, 40) -- 2 cột nút
        layout.CellPadding = UDim2.new(0.02, 0, 0.05, 0)

        local tabBtn = Instance.new("TextButton", sidebar)
        tabBtn.Size = UDim2.new(0.7, 0, 0.7, 0)
        tabBtn.SizeConstraint = Enum.SizeConstraint.RelativeXX -- Luôn tròn
        tabBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        tabBtn.BackgroundTransparency = 0.8
        tabBtn.Text = iconText or name:sub(1,1)
        tabBtn.TextScaled = true
        tabBtn.Font = Enum.Font.GothamBold
        tabBtn.TextColor3 = TextColor
        Instance.new("UICorner", tabBtn).CornerRadius = UDim.new(1, 0)

        tabBtn.MouseButton1Click:Connect(function()
            for _, v in pairs(pages:GetChildren()) do v.Visible = false end
            page.Visible = true
        end)

        local elements = {}
        function elements:AddButton(text, callback)
            local btn = Instance.new("TextButton", page)
            btn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            btn.BackgroundTransparency = 0.4
            btn.Text = text
            btn.Font = Enum.Font.GothamSemibold
            btn.TextColor3 = TextColor
            btn.TextScaled = true
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0.2, 0)
            btn.MouseButton1Click:Connect(callback)
            
            -- Padding cho chữ không bị sát viền
            local uipadding = Instance.new("UIPadding", btn)
            uipadding.PaddingTop = UDim.new(0.2, 0)
            uipadding.PaddingBottom = UDim.new(0.2, 0)
        end
        
        if #sidebar:GetChildren() == 2 then page.Visible = true end
        return elements
    end

    -- Kéo thả hỗ trợ cả Touch (Mobile)
    local dragging, dragStart, startPos
    main.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true; dragStart = input.Position; startPos = main.Position
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end
    end)

    return Library
end

-- Cách sử dụng
local win = Library:CreateWindow()
local mainTab = win:CreateTab("Home", "H")
mainTab:AddButton("Test Mobile 1", function() print("Ok!") end)
mainTab:AddButton("Destroy UI", function() CleanupOldUI() end)
